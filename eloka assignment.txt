class NotesApplication {
	/**
	*
	* A class that takes in a parameter 
	* Creates a list
	**/
	constructor (author) {
		this.author = author;
		this.notes = [];
	}

	create(note_content) {
	    this.notes.push(note_content);
	}

	
	list(){
	    for (let i = 0; i < this.notes.length; i++) {		
	   	console.log(i);
	   	console.log(this.note[i]);
	   	console.log("By author " + this.author);
	    }
	}
	
	
	get(note_id) {
		for (let i=0; i<this.notes.length; i++) {
			return this.notes[i];
		}
	}
	
	
	search(search_text) {
		for (let i = 0; i < this.notes.length; i++) {
			if (search_text in this.notes.length[i]) {
				 a = "Showing results for search" + this.search_text;
				 b = this.notes[i];
				 c = "Arthor " + this.arthor;
				 return a + b + c;
				
			}
		}
	}
 
	
	delete(note_id) {	
		delete this.notes[note_id];
		}  
	
	edit(notes_id, new_content) {
		this.note[note_id] = new_content;
		}
}